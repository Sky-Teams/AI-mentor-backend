import { Reference } from "src/modules/references/domain/reference";

export class ChicagoFullNoteCitationFormatter {
    constructor(
        private readonly 
    ){}

  public formatCitation(reference: Reference, sectionId: string) {

    return "";
  }
}
